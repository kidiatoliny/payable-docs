---
import { cn } from '@/lib/utils';
import type { NavGroup } from '@/lib/nav';

interface Props {
  nav: NavGroup[];
  currentSlug: string;
}

const { nav, currentSlug } = Astro.props;
---

<aside
  class="sticky top-16 hidden h-[calc(100vh-4rem)] w-64 shrink-0 overflow-y-auto bg-sidebar px-4 py-6 lg:block"
>
  <nav class="flex flex-col gap-6">
    {
      nav.map((group) => (
        <div>
          <p class="mb-2 px-2 text-xs font-bold uppercase tracking-wider text-foreground">
            {group.label}
          </p>
          <ul class="flex flex-col gap-0.5">
            {group.items.map((item) => (
              <li>
                <a
                  href={item.href}
                  class={cn(
                    'block rounded-md px-2 py-1.5 text-sm text-sidebar-foreground transition-colors hover:bg-sidebar-accent hover:text-sidebar-accent-foreground',
                    item.id === currentSlug &&
                      'bg-sidebar-accent font-medium text-sidebar-accent-foreground',
                  )}
                >
                  {item.title}
                </a>
              </li>
            ))}
          </ul>
        </div>
      ))
    }
  </nav>
</aside>
